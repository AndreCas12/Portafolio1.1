
window.onload = function () {
    setTimeout(function () {
        window.location.href = "principal.html";  // Redirige a otra página (ajusta la URL si es necesario)
    }, 3000);  // 3 segundos de carga (ajusta el tiempo si lo necesitas)
};
